# Create: conda env create -f environment.yml
# Remove: conda remove --name <env_name> --all

name: ooi_env
channels:
  - conda-forge
dependencies:
  - python=3.11
  - numpy
  - netcdf4
  - obspy
  - requests
  - pip
  - pip:
      # EarthScope Dropoff CLI (`es`) — cloud drop-off replacing miniseed2dmc
      - earthscope-cli


